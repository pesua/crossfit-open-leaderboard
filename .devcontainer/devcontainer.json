{
    "name": "openscrollytelling",
    "image": "mcr.microsoft.com/devcontainers/universal:2",
    "runArgs": ["--cap-add=NET_ADMIN", "--cap-add=NET_RAW"],
    "containerEnv": {
        "CLAUDE_CONFIG_DIR": "/home/codespace/.claude"
    },
    "postCreateCommand": "npm install -g @anthropic-ai/claude-code && sudo apt-get update && sudo apt-get install -y chromium && claude mcp add chrome-devtools -- npx -y chrome-devtools-mcp@latest",
    "workspaceMount": "source=${localWorkspaceFolder},target=/workspace,type=bind,consistency=delegated",
    "workspaceFolder": "/workspace",
    "mounts": [
        "source=${localEnv:HOME}/.gitconfig,target=/home/codespace/.gitconfig,type=bind,readonly",
        "source=${localEnv:HOME}/.ssh,target=/home/codespace/.ssh,type=bind,readonly"
    ],
    "postStartCommand": "sudo iptables -P OUTPUT ACCEPT && sudo iptables -P INPUT ACCEPT",
    "waitFor": "postStartCommand",
    "forwardPorts": [3000, 5173, 8000, 8080]
}
